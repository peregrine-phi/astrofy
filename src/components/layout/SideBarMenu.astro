---
import { getRelativeLocaleUrl } from "astro:i18n";
import { useTranslations } from "../../i18n/utils";
const { sideBarActiveItemID } = Astro.props;
const activeClass = "bg-base-300"; // For primary color replace with `active` class

const lang = Astro.currentLocale || "en";
const t = useTranslations(lang as any);
---

<ul class="menu grow shrink menu-md overflow-y-auto">
    <li><a class="py-3 text-base" id="home" href={getRelativeLocaleUrl(lang, "")}>{t('nav.home')}</a></li>
    <li><a class="py-3 text-base" id="projects" href={getRelativeLocaleUrl(lang, "projects")}>{t('nav.projects')}</a></li>
    <li><a class="py-3 text-base" id="services" href={getRelativeLocaleUrl(lang, "services")}>{t('nav.services')}</a></li>
    <li><a class="py-3 text-base" id="tutorials" href={getRelativeLocaleUrl(lang, "tutorials")}>{t('nav.store')}</a></li>
    <li><a class="py-3 text-base" id="blog" href={getRelativeLocaleUrl(lang, "blog")}>{t('nav.blog')}</a></li>
    <li><a class="py-3 text-base" id="cv" href={getRelativeLocaleUrl(lang, "cv")}>{t('nav.cv')}</a></li>
    <li>
        <a
            class="py-3 text-base"
            href="mailto:contact.manuelernestog@gmail.com"
            target="_blank"
            referrerpolicy="no-referrer-when-downgrade">{t('nav.contact')}</a
        >
    </li>
</ul>

<script define:vars={{ sideBarActiveItemID: sideBarActiveItemID, activeClass: activeClass }}>
const activeItemElem = document.getElementById(sideBarActiveItemID);
activeItemElem && activeItemElem.classList.add(activeClass);
</script>
